# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://username:password@localhost:5432/vertexgsm?schema=public"

# NextAuth Configuration
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Stripe (Standard Payments)
STRIPE_SECRET_KEY="sk_live_" # server-side only
STRIPE_WEBHOOK_SECRET="whsec_" # /api/webhooks/stripe (balance + transactions)

# Stripe (Subscriptions)
STRIPE_PRO_PRICE_ID="price_pro_"
STRIPE_ENTERPRISE_PRICE_ID="price_enterprise_"
STRIPE_SUBSCRIPTIONS_WEBHOOK_SECRET="whsec_subscriptions_" # /api/webhooks/stripe-subscription
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_"

# Encryption (AES-256-GCM for API keys)
ENCRYPTION_KEY="generate-with: openssl rand -hex 32"

# Optional Future Email
# SMTP_HOST=""
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""

# Observability (optional)
# SENTRY_DSN=""
# POSTHOG_KEY=""

# Feature Flags
# ENABLE_SUBSCRIPTIONS="true"

# Note: Webex & DHRU credentials stored per-reseller in DB; do not place here.
